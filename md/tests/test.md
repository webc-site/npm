# Title

```mermaid
graph TD
  A --> B
```

Some text.

```javascript
const a = 1;
console.log(a);
```

Unclosed:

```mermaid
graph LR
  C --> D
```

Four backticks:

```mermaid
graph TD
  E --> F
```

No language code block:

```
const b = 2;
```
